#include "ScoringMatrix.h"
#include "Sequence.h"

class Align {
private:

public:
	Align();

	int globalAlignment(Sequence sequenceA, Sequence sequenceB, ScoringMatrix scoringMatrix)
	{
		int matrix[sequenceA.size()][sequenceB.size()];

		for(int i=0; i<sequenceA.size(); i++){
			matrix[][]
		}
		for(int i=0; i<sequenceB.size(); i++){
			
		}
	}
}
